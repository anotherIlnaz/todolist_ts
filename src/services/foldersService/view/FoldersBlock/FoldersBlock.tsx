import React, { FC } from 'react';
import { Wrapper } from './FoldersBlock.styled';
import { FoldersBlockProps } from './FoldersBlock.types';

export const FoldersBlock: FC<FoldersBlockProps> = ({}) => {
  return <Wrapper></Wrapper>
};
