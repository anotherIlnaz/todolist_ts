import styled from "styled-components";

const Container = styled.div`
   display: flex;
   flex-direction: row;
   margin: 0 2rem;
`;

export const MainPage = () => {
   return (
      <>
         <Container>
         </Container>
      </>
   );
};
